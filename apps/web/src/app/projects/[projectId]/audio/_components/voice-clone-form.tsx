import { Id } from "@repo/convex/dataModel";

export interface VoiceCloneFormProps {
  projectId: Id<"project">;
}

export function VoiceCloneForm({ projectId }: VoiceCloneFormProps) {
  return <div></div>;
}
