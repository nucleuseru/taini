name: Build and Push LTX-2.3

on:
  push:
    branches:
      - main
    paths:
      - "packages/ltx-2.3/**"
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          context: ./packages/ltx-2.3
          file: ./packages/ltx-2.3/Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/ltx-2.3:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/ltx-2.3:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
